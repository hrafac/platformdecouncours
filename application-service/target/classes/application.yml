server:
  port: 8084

spring:
  application:
    name: application-service
  datasource:
    url: jdbc:postgresql://localhost:5432/recruitment_db
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: achraf.melfout@etud.iga.ac.ma
    password: lythtliqkmpyhcwn
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          ssl:
            trust: smtp.gmail.com

notifications:
  admin:
    email: achraf.melfout@etud.iga.ac.ma

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
instance:
    prefer-ip-address: true